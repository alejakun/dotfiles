#!/bin/sh

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    source "~/.dotfiles/bin/set_env.sh"
fi

echo "🖥  Setting up Vim..."

# Installing Spacevim from: https://spacevim.org

echo "📦 Installing SpaceVim"
curl -sLf https://spacevim.org/install.sh | bash
