#!/bin/sh

echo "🖥  Setting up Terminal..."

echo "🚛 Opening themes"

# Removes ~/.ssh/config if it exists and symlinks the .ssh/config file from the .dotfiles
cd "$DOTFILES/macos/themes/"
open "*.terminal"
cd -

echo "🖥  Setting up iTerm2..."

echo "🚛 Opening themes"

open "$DOTFILES/macos/themes/"
open "*.itermcolors"
cd -